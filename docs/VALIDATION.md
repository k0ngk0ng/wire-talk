# 当前验收记录

## 已完成的本地验证

- `go test -race ./...` 与 `go vet ./...` 首轮通过（macOS arm64）。
- 三个真实 UDP socket 的发现、直接音频帧传输，入口节点关闭后其余节点继续互通。
- 报文篡改、错误密钥、重放、过期/未来时间戳和短包被拒绝。
- 混音限幅、乱序旧帧丢弃、队列上限及欠载静音。
- 同一配置的独占锁；状态查询不会结束会话；控制接口要求授权令牌。
- miniaudio Null 驱动的真实 C/Go 双工回调：关闭设备后采集停止。
- 使用仅测试构建的 Null 驱动，本机完整验证前台 Ctrl+C、后台启动/停止/重启、独占锁、静音以及退出 watch 不影响会话。
- 本机设备枚举仅发现 DELL U2717D 和 Mac mini Speakers 输出，没有输入设备。

## 仍需验证/完成

- 实际麦克风、扬声器双端对话，以及扬声器保护的听感、延迟和噪声环境表现。
- 首轮 GitHub Actions CI（35679989055）在 macOS arm64/amd64、Linux arm64/amd64、Windows amd64 全部通过原生测试、构建、安装包实际自更新。服务/ACL 的 CI（35680385996）也已五平台通过。
- 原生服务自启和设备断开恢复：已实现系统服务定义，尚需实际用户登录会话验收。Windows ACL 已通过原生初始化与自更新测试；服务注册尚未实机验收。
- 发布后 Homebrew/Scoop 安装与线上更新。
- 真实跨机器网络、IPv6、长时间运行以及 32 成员负载。

测试中的 PCM 是合成数据；网络帧成功不等于真实音频设备已验收。当前不提供
NAT 自动打洞或语音中继，不应把可达 UDP 的测试解释为任意互联网网络可直连。
